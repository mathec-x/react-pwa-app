{
  "name": "react-pwa-app",
  "version": "0.1.1",
  "author": "mathec",
  "license": "ISC",
  "keywords": ["react", "nextjs", "pwa", "add to home screen"],
  "private": false,
  "main": "dist/index.js",
  "types": "./lib/index.d.ts",
  "module": "dist/index.js",
  "files": ["dist", "README.md"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mathec-x/react-pwa-app.git"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "build": "rd /s /q dist && npm run copy",
    "copy": "npx babel src/lib --out-dir dist --copy-files",
    "deploy": "npm run build && npm publish", 
    "start": "set PORT=1111 && react-scripts start",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.14.5",
    "@babel/core": "^7.14.5",
    "@babel/preset-env": "^7.14.5"
  }
}
